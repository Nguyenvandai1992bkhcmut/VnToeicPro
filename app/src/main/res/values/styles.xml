<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

    </style>


    <style name="PauseDialogAnimation">
        <item name="android:windowEnterAnimation">@anim/right_left_in</item>
        <item name="android:windowExitAnimation">@anim/right_left_out</item>
    </style>

    <style name="PauseDialogFavorit">
        <item name="android:windowEnterAnimation">@anim/top_bot</item>
        <item name="android:windowExitAnimation">@anim/top_bot_out</item>
    </style>

    <style name="ActivityDialog" parent="@android:style/Theme.Dialog">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:windowBackground">@drawable/rect_card_dialog</item>
        <item name="android:windowEnterAnimation">@anim/top_bot</item>
        <item name="android:windowExitAnimation">@anim/top_bot_out</item>
    </style>
</resources>
