<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

    </style>

    <style name="dialog_tran" parent="android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:backgroundDimAmount">0.4</item>
    </style>

    <style name="PauseDialogAnimation">
        <item name="android:windowEnterAnimation">@anim/right_left_in</item>
        <item name="android:windowExitAnimation">@anim/right_left_out</item>
    </style>

    <style name="PopUpSetting">
        <item name="android:windowEnterAnimation">@anim/scale_question_in</item>
        <item name="android:windowExitAnimation">@anim/scale_question_out</item>
    </style>

    <style name="PauseDialogFavorit">
        <item name="android:windowEnterAnimation">@anim/top_bot</item>
        <item name="android:windowExitAnimation">@anim/top_bot_out</item>
    </style>

    <style name="ActivityDialog" parent="@android:style/Theme.Holo.Dialog.NoActionBar">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowBackground">@drawable/rect_card_dialog</item>

        <item name="android:windowEnterAnimation">@anim/top_bot</item>
        <item name="android:windowExitAnimation">@anim/top_bot_out</item>
    </style>

    <declare-styleable name="StatableViewGroup">
        <attr name="state_pressed" format="boolean"/>
    </declare-styleable>

    <style name="DialogNoBackGround" parent="@android:style/Theme.Holo.Dialog.NoActionBar">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowBackground">@color/none</item>
    </style>
</resources>
